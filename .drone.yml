---
kind: pipeline
type: docker
name: default
platform:
  os: linux
  arch: ppc64le
steps:
- name: test
  image: alpine
  environment:
    GOOS: linux
    GOARCH: ppc64le
  commands:
  - echo "It worked!"
  - mkdir test
  - cd test
  - arch
  - uname -r
 
