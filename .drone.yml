---
kind: pipeline
type: docker
name: default
platform:
  os: linux
  arch: arm64
steps:
- name: test
  image: alpine
  commands:
  - echo "It worked!"
  - mkdir test
  - cd test
  - arch
  - uname -r
 
